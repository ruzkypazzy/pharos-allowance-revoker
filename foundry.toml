# Minimal foundry.toml for pharos-allowance-revoker
# This is an RPC-only analyzer, not a Solidity project, but cast requires
# a config to find the project root.
[profile.default]
src = "scripts"
out = "out"
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
